import { Button } from "@/app/components/button";

export const ContactFormButton = () => (
  <div data-aos="fade-up">
    <Button filled={false} itemType="submit">
      Send!
    </Button>
  </div>
);
